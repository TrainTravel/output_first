## Change
In `src/components/journal/SelfCompassionSeed.tsx`, line 58, increase the font size of the daily self-compassion phrase `<p>` from `text-sm` to `text-base`.

## Why
The selected phrase text is currently `text-sm` (0.875rem / 14px). For a grounding, compassionate message meant to be read slowly and felt, a slightly larger size (`text-base` / 16px) improves readability and emotional presence without disrupting the card layout.

## Verification
- Preview the Self Compassion Seed card on the home screen.
- Confirm the phrase text renders at a comfortable, readable size.